spring:
  application:
    name: service
  cloud:
    config:
      enabled: false
  liquibase:
    enabled: false

management:
  metrics:
    export:
      influx:
        enabled: false

eureka:
  client:
    enabled: false

logging:
  config: classpath:logback.xml
